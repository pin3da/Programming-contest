/*
ID: manuel.5
PROG: gift1
LANG: C++
*/
#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include <map>
#include <string>
#include <cstdio>

#define For(i,n) for(int i=0; i<(n); ++i)
#define pname "gift1"

using namespace std;

int main() {
    ofstream fout (pname".out");
    ifstream fin (pname".in");
    int numper;fin>>numper;
	map<string,int> cod;
	map<int,string> decod;
	int persons[numper];
	string tmp;
	For(i,numper){
		
	}
	

    return 0;
}

