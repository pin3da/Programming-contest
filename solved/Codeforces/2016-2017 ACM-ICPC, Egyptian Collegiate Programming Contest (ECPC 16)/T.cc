#include <bits/stdc++.h>

using namespace std;
#define debug(x) cout << #x " = " << (x) << endl

void solve() {

}

int main() {
#ifndef LOCAL
  freopen("TTT.in", "r", stdin);
#endif

  int t; scanf("%d", &t);
  while(t--) solve();

  return 0;
}
