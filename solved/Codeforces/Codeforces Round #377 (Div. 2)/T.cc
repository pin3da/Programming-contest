#include <bits/stdc++.h>

using namespace std;

#define debug(x) cout << #x << (x) << endl

int main() {
#ifndef LOCAL
  ios_base::sync_with_stdio(false);cin.tie(NULL);
#define endl '\n'
#endif

  return 0;
}
