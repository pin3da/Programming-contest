#include <bits/stdc++.h>
#include "./template.cc"
using namespace std;

struct Solution {

};

struct Input {

};

int main() {
    vector<Input> in = {

    };

    for (auto &it : in) {
        Solution sol;
        // TODO: Method.
        sol.method(in);
    }
}