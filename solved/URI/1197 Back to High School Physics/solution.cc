#include <bits/stdc++.h>
#define debug(x) cout << __LINE__<< ":" #x " = " << (x) << endl
#define endl '\n'

using namespace std;

int main() {
  ios_base::sync_with_stdio(false);cin.tie(NULL);
  long long v, t;
  while (cin >> v >> t)
    cout << v * t * 2 << endl;
  return 0;
}
