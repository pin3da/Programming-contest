#include <bits/stdc++.h>

using namespace std;

set<string> mmin;
set<string> mmax;


#define debug(x) cout << #x << " = " << (x) << endl
int main() {
#ifdef LOCAL
#define endl '\n'
  ios_base::sync_with_stdio(false); cin.tie(NULL);
#endif

  return 0;
}
