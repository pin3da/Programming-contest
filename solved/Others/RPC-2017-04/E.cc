#include <bits/stdc++.h>
using namespace std;

#define debug(x) cout << #x << " = " << (x) << endl

void solve() {
  long long n, k;
  cin >> n >> k;
  vector<long long> a(n);
  for (auto &i : a) cin >> i;
  puts("Yes.");
}

int main() {
  int t; cin >> t;
  while (t--) {
    solve();
  }
  return 0;
}
